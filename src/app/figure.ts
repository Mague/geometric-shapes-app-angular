export interface Figure {
	id: number | null;
  	width: number | null;
    height: number | null;
    base: number | null;
    radius: number | null;
    type: string | null;

	/* constructor(
		id: number,width: number,height: number,base: number,radius: number,type: string
	) {
		this.id=id;
		this.width=width;
		this.height=height;
		this.base=base;
		this.radius=radius;
		this.type=type;
	} */
}
